#! /usr/bin/env node

// Lets use Coffee-Script!
coffee = require('coffee-script');
require('coffee-script/register');
// Make sure we think we're in the right directory.
process.chdir(__dirname.replace(/bin$/,""));
// Start
require('../server');